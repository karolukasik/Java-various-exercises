package com.karolina.sortingalgorithms;

import java.util.ArrayList;
import java.util.Random;

public class SortingAlgorithmsProject {
    public static void main(String[] args) {
        // QuickSort<Integer> quickSorter = new QuickSort<>();
        // SelectionSort<Integer> selectionSorter = new SelectionSort<>();
        // BubbleSort<Integer> bubbleSorter = new BubbleSort<>();
        // InsertionSort<Integer> insertionSorter = new InsertionSort<>();

        // ArrayList<Integer> listForQuickSort = new ArrayList<>();
        // ArrayList<Integer> listForSelectionSort = new ArrayList<>();
        // ArrayList<Integer> listForBubbleSort = new ArrayList<>();
        // ArrayList<Integer> listForInsertionSort = new ArrayList<>();

        // Random rnd = new Random();

        // for (int i = 0; i < 10000; i++) {
        //     int value = rnd.nextInt(10000);
        //     listForQuickSort.add(value);
        //     listForSelectionSort.add(value);
        //     listForBubbleSort.add(value);
        //     listForInsertionSort.add(value);
        // }

        // quickSorter.printSortingTime(listForQuickSort);
        // selectionSorter.printSortingTime(listForSelectionSort);
        // bubbleSorter.printSortingTime(listForBubbleSort);
        // insertionSorter.printSortingTime(listForInsertionSort);

        System.out.println("\"\\Hello World\\\"");

    }

}
